# saassy
Saassy is a framework/boilerplate for SaaS platforms (including stripe for payment, pocketbase for users, a frontend website, a database for tasks per user management, a service to calculate payload for billing, a dashboard backend the monitors the spawn and running of worker instances - for any kind of docker 
